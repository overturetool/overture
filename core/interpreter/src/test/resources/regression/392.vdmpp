class A
instance variables 
private x : nat := 1;
private other : [A] := nil;

inv if other = nil then true else other.x = 1;

operations
public static test : () ==> ()
test() == let a = new A() 
					in
					 skip

end A

