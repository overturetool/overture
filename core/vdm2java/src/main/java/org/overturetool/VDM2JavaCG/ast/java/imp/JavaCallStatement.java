// this file is automatically generated by treegen. do not modify!

package org.overturetool.VDM2JavaCG.ast.java.imp;

// import the abstract tree interfaces
import org.overturetool.VDM2JavaCG.ast.java.itf.*;

// import java collection types
import java.util.*;

public class JavaCallStatement extends JavaStatement implements IJavaCallStatement
{
	// private member variable (name)
	private IJavaIdentifier m_name = null;

	// public operation to retrieve the embedded private field value
	public IJavaIdentifier getName()
	{
		return m_name;
	}

	// public operation to set the embedded private field value
	public void setName(IJavaIdentifier p_name)
	{
		// consistency check (field must be non null!)
		assert(p_name != null);

		// instantiate the member variable
		m_name = p_name;

		// set the parent of the parameter passed
		p_name.setParent(this);
	}

	// private member variable (Args)
	private List<IJavaExpression> m_Args = new Vector<IJavaExpression>();

	// public operation to retrieve the embedded private field value
	public List<IJavaExpression> getArgs()
	{
		return m_Args;
	}

	// public operation to set the embedded private field value
	public void setArgs(List<IJavaExpression> p_Args)
	{
		// consistency check (field must be non null!)
		assert(p_Args != null);

		// instantiate the member variable
		m_Args = p_Args;

		// set the parent of each element in the sequence parameter passed
		for (IJavaNode lnode: p_Args) lnode.setParent(this);
	}

	// default constructor
	public JavaCallStatement()
	{
		super();
		m_name = null;
		m_Args = null;
	}

	// auxiliary constructor
	public JavaCallStatement(
		IJavaIdentifier p_name,
		List<IJavaExpression> p_Args
	) {
		super();
		setName(p_name);
		setArgs(p_Args);
	}

	// visitor support
	public void accept(IJavaVisitor pVisitor) { pVisitor.visitCallStatement(this); }

	// the identity function
	public String identify() { return "JavaCallStatement"; }
}
