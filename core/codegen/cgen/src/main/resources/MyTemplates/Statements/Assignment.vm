## If the target is a map sequence state designator, e.g. counters.put("c1", 4),
## then only the target should be generated.
##
#if ($CFormat.isMapSeq($node.getTarget()))
	$CFormat.format($node.getTarget());##
#else
	$CFormat.format($node.getTarget()) = $CFormat.format($node.getExp());##
#end