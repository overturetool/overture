#set( $args = $XFormat.formatArgs($node.getArgs()) )
#set( $field = $node.getFieldName() )
#set( $obj = $XFormat.format($node.getObj()) )
##
#set( $return = "")
#if (!$XFormat.isVoidType($node.getType()))
	#set( $return = "return ")
#end
##
${return}${obj}.${field}(${args});