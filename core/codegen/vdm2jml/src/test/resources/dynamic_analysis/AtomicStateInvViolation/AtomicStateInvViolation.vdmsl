module Entry

imports from IO all
definitions

state St of
  x : nat
  init s == s = mk_St(1)
  inv s == s.x = 1
end

operations

Run : () ==> ?
Run () ==
(
  atomic
  (
    x := 2;
  );
  atomic
  (
    x := 1;
  );
  
  return 2;
);

end Entry
