module Entry

exports all
imports from IO all
definitions

operations

Run : () ==> ?
Run () ==
(
  let - = f() in skip;
  IO`println("Done! Expected no violations");
  return 0;
);

functions

f :  () -> real
f () == 
let 1.5 = 1.5
in
  1.5;

end Entry	