module Entry

exports all
imports from IO all
definitions

operations

Run : () ==> ?
Run () ==
(
  let - = f(1) in skip;
  IO`println("Done! Expected no violations");
  return 0;
);

functions

f :  nat -> nat
f (1) == 
 2;

end Entry	