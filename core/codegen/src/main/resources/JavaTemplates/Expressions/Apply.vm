##Example1: t.getX(1, 1)
##Example2: [1,2](1)
##Example3: "kitten"(3)
##
#set( $root = $JavaFormat.format($node.getRoot()) )
#set( $args = $JavaFormat.formatArgs($node.getArgs()) )
##
$root##
##
#if ($JavaFormat.isSeqEnum($node.getRoot()))
#set( $index = $JavaFormat.format($node.getArgs().get(0)))
.get(Utils.index($index))##
##
#elseif ($JavaFormat.isStringLiteral($node.getRoot()))
#set( $index = $JavaFormat.format($node.getArgs().get(0)))
.charAt(Utils.index($index))##
##
#else
($args)##
#end