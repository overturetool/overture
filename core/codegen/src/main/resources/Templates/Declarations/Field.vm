#set( $final = "")
#if ($node.getFinal())
	#set( $final = "final")
#end
##
#set( $static = "")
#if ($node.getStatic())
	#set( $static = "static")
#end
##
$node.getAccess() $static $final $JavaFormat.format($node.getType()) $node.getName() $JavaFormat.formatInitialExp($node.getInitial());