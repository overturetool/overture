class Entry

values

s = [1,2,3];

operations 

public static Run : () ==> ?
Run () ==
	Test();
		
public static Test : () ==> seq of nat 
Test () ==
(
	dcl a : seq of nat := [ x | x in set elems s & 1 = 1 ];
	return a;
);

end Entry