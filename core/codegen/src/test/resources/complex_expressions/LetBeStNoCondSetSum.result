import org.overture.codegen.javalib.*;

import java.util.*;


public class Entry {
    public Entry() {
    }

    public static Object Run() {
        return sum(SetUtil.set(1L, 2L, 3L, 4L, 5L));
    }

    public static Number sum(VDMSet xs) {
        if (xs.isEmpty()) {
            return 0L;
        } else {
            VDMSet set_1 = xs.clone();
            Iterator iterator_1 = set_1.iterator();
            Number x = null;
            boolean success_1 = false;

            while (iterator_1.hasNext() && !(success_1)) {
                x = ((Number) iterator_1.next());
                success_1 = true;
            }

            if (!(success_1)) {
                throw new RuntimeException(
                    "Let Be St found no applicable bindings");
            }

            return x.longValue() +
            sum(SetUtil.diff(xs.clone(), SetUtil.set(x))).longValue();
        }
    }
}
##########