import org.overture.codegen.javalib.*;

import java.util.*;


public class Entry {
    private static final VDMSeq s = SeqUtil.seq(1L, 2L, 3L);

    public Entry() {
    }

    public static Object Run() {
        return Test();
    }

    public static VDMMap Test() {
        VDMMap mapCompResult_1 = MapUtil.map();
        VDMSet set_1 = SeqUtil.elems(s.clone());

        for (Iterator iterator_1 = set_1.iterator(); iterator_1.hasNext();) {
            Number x = ((Number) iterator_1.next());

            if (1L == 1L) {
                mapCompResult_1.put(x, x.longValue() + 1L);
            }
        }

        VDMMap a = mapCompResult_1;

        return a.clone();
    }
}

##########
