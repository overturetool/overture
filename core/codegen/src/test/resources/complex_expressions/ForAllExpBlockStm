class Entry

values

s = [1,2,3];

operations 

public static Run : () ==> ?
Run () ==
	Test();
		
public static Test : () ==> bool 
Test () ==
(
	dcl a : bool := forall x, y in set {1,2,3} & x + y > 3;
	return a;
);

end Entry