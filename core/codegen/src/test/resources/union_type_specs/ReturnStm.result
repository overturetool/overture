import org.overture.codegen.runtime.*;

import java.util.*;


public class Entry {
    public Entry() {
    }

    public static Object Run() {
        return f(SetUtil.set(Tuple.mk_(2.6, 2.6)));
    }

    private static Number f(final VDMSet tups) {
        Tuple e = null;
        Boolean success_1 = false;
        VDMSet set_1 = Utils.clone(tups);

        for (Iterator iterator_1 = set_1.iterator();
                iterator_1.hasNext() && !(success_1);) {
            e = ((Tuple) iterator_1.next());
            success_1 = true;
        }

        if (!(success_1)) {
            throw new RuntimeException("Let Be St found no applicable bindings");
        }

        Object letBeStExp_1 = (1L == 1L) ? 2.5 : ((Number) e.get(0));

        return ((Number) letBeStExp_1);
    }

    public String toString() {
        return "Entry{}";
    }
}

##########
