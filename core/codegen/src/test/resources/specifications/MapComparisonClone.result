import org.overture.codegen.runtime.*;

import java.util.*;


public class A {
    private static final VDMMap a = MapUtil.map(new Maplet(1L, 2L));
    private static final boolean b = MapUtil.equals(a, op());
    private static final boolean c = !(MapUtil.equals(a, op()));

    public A() {
    }

    public static VDMMap op() {
        return MapUtil.map(new Maplet(1L, 2L), new Maplet(3L, 4L));
    }
}

##########
