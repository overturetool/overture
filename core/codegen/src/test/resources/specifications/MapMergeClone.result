import org.overture.codegen.javalib.*;

import java.util.*;


public class A {
    private static final VDMMap a = MapUtil.map(new Maplet(0L, 0L));
    private static final VDMMap x = MapUtil.merge(SetUtil.set(a.clone(), m1()));

    public A() {
    }

    public static VDMMap m1() {
        return MapUtil.map(new Maplet(3L, 4L), new Maplet(5L, 6L));
    }
}

##########
