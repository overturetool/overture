import utils.*;

import java.util.*;


public class A {
    private static final List<Object> as = Utils.seq();
    private static final List<Long> bs = Utils.seq(1L, 2L);
    private static final List<Double> cs = Utils.seq(1.2, 3.3);
    private static final List<Boolean> ds = Utils.seq(true, false);
    private static final String es = new String(new char[] { 'a', 'b' });
    private static final List<String> fs = Utils.seq("a", "b");

    public void op() {
        List<Object> as_ = Utils.<Object>reverse(as);

        List<Long> bs_ = Utils.<Long>reverse(bs);

        List<Double> cs_ = Utils.<Double>reverse(cs);

        List<Boolean> ds_ = Utils.<Boolean>reverse(ds);

        String es_ = new StringBuilder(es).reverse().toString();

        List<String> fs_ = Utils.<String>reverse(fs);

        //Skip;
    }
}