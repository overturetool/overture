import utils.*;

import java.util.*;


public class A {
    private static final List as = Utils.seq();
    private static final List bs = Utils.seq(1L, 2L);
    private static final List cs = Utils.seq(1.2, 3.3);
    private static final List ds = Utils.seq(true, false);
    private static final String es = new String(new char[] { 'a', 'b' });
    private static final List fs = Utils.seq("a", "b");

    public void op() {
        List as_ = Utils.reverse(as);

        List bs_ = Utils.reverse(bs);

        List cs_ = Utils.reverse(cs);

        List ds_ = Utils.reverse(ds);

        String es_ = new StringBuilder(es).reverse().toString();

        List fs_ = Utils.reverse(fs);

        //Skip;
    }
}
##########