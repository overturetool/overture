class Entry

values 

xs : set of (nat | A) = {1,2,3,4,mk_A(5), 6, 7, 8}

types

A :: a : int;  

operations

id : nat ==> nat
id (x) == return x;

traces

T1:
let mk_A(a) in set xs be st a = 5
in
  id(a);

end Entry