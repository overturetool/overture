class Entry

types

public NatOrBool = nat | bool

values

x : NatOrBool = true;

operations

public static Run : () ==> ?
Run () ==
	if not x then
		return 1
	elseif x then
		return 2
	elseif x then
		return 3
	else
		return 4; 

end Entry