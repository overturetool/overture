class Entry

values

s = [4,5,6];

operations 

public static Run : () ==> ?
Run () ==
	Test();
		
public static Test : () ==> set of nat 
Test () ==
(
	dcl a : set of nat := { x | x in set elems s & 1 = 1 };
	return a;
);

end Entry