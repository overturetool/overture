class Entry

operations

public static Run : () ==> ?
Run () ==
( 
  dcl a : nat := 1;
  dcl b : set of nat := {x+a | x in set {1,2,3}};
  dcl c : nat := 2;
	  
     (
       dcl d : nat := 10;
       dcl e : set of nat := {x+d | x in set {4,5,6}};
       dcl f : nat := 20;
		 
       return b union e;
      )
)
end Entry
