class Entry

operations

public static Run : () ==> ?
Run () ==
trap mk_(a,a)
with
  let mk_(a,a) = mk_(a,a)
  in
    return a
in
  let mk_(a,a) = mk_(1,1)
  in
    if false then exit mk_(0,0) else return a;

end Entry