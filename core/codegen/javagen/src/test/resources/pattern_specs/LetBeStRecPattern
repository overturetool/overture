class Entry

values 

xs : set of (nat | A) = {1,2,3,4,mk_A(5), 6, 7, 8}

types

A :: a : int;  

operations

public static Run : () ==> ?
Run () == 
let mk_A(a) in set xs be st a = 5
in
  return a;

end Entry