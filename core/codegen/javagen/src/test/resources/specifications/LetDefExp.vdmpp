class A

operations

public op : () ==> nat
op () == 
	return let x = 1 in x;

functions

g :  nat -> nat
g (-) == 1;

public static f: nat -> nat
f(x) ==
	let x1 = g(1),
			x2 = g(2),
			x3 = g(3)
	in
		x + x1 + x2 + x3;

end A