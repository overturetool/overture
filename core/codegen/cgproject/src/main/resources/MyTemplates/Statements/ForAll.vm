#set( $iterator = $XFormat.nextVarName($TempVar.getIteratorNamePrefix()) )
#set( $exp = $XFormat.format($node.getExp()) )
#set( $var = $XFormat.format($node.getPattern()) )
#set( $elementType = $XFormat.formatElementType($node.getExp().getType()) )
#set( $body = $XFormat.format($node.getBody()) )
##
for (Iterator ${iterator} = ${exp}.iterator(); ${iterator}.hasNext();)
{
	${elementType} ${var} = (${elementType}) ${iterator}.next();
	${body}
}