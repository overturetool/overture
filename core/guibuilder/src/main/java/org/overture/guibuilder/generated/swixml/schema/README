These classes were generated using the JAXB tool and the corresponding XML schema language.

CLASSES:

swixml - generated classes using swixml.xsd file stored in / resources

---------

These classes were created using JAXB XML schema file and the matching of language.

CLASSES:

swixml - generated using the swixml.xsd file stored in / resources