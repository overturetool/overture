--
-- MapTest.vdmsl
--
operations

maptest() m : map nat to nat
  post m = { 1|->2, 2|->2, 3|->1 };

domaintest() m : set of nat
  post m = dom { 1|->2, 2|->2, 3|->1 };

-- domaintest() m : set of nat
--   post m = dom { 1|->(2|->3), 2|->(2|->2), 3|->(1|->3) };
 
rangetest() m : set of nat
  post m = rng { 1|->2, 2|->2, 3|->1 };

-- rangetest() m : set of map nat to nat
--   post m = rng { 1|->(2|->3), 2|->(2|->2), 3|->(1|->3) }

mapu() m : map nat to nat
  post m = { 1|->3, 2|->1, 3|->4} munion { 4|->4 };

seqmod() m : seq of nat
  post m = [10,20, 30,40, 50] ++ {2|->2, 4|->4};
  
mapoverride() m : map nat to nat
  post m = {1|->10, 2|->20, 3|->30, 4|->40} ++ {2|->25, 5|->50};

mapdomrest() m : map nat to nat
  post m = {1,3}<:{1|->10, 2|->20,3|->30,4|->40};
  
mapdomrestby() m : map nat to nat
  post m = {1,3}<-:{1|->10, 2|->20,3|->30,4|->40};
 
mapranrest() m : map nat to nat
  post m = {1|->10, 2|->20,3|->30,4|->40}:>{10,40};
  
mapranrestby() m : map nat to nat
  post m = {1|->10, 2|->20,3|->30,4|->40}:->{10,40};
  
mapapp() m : nat
  post m = {1->11,2|->21,3|->31,4|->41}(3)
  