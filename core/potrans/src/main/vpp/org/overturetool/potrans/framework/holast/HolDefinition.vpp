class HolDefinition is subclass of MLExpression

instance variables

operations

public setTypeInformation : HolTypeDescription ==> ()
setTypeInformation (-) == 
	skip;

public provides : () ==> set of seq of char
provides() == is subclass responsibility;

public requires : () ==> set of seq of char
requires() == is subclass responsibility;

end HolDefinition
