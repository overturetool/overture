-- [EXP PROOF] "Proof ob 1"
-- 
--
-- 
-- Test that makes the compiler exercise the apply expression
-- proof generation.
--
types 
    B :: k : bool;
    
values
    a: bool = g();
    c: B = mk_B(f());
    d: seq of bool = mk_(false, false);
    e: B = mk_B(d.#1); 
    
functions

    f: () -> bool
    f() == true;
    
    g: () -> bool
    g() == f(); 