To generate the OS X icon, use an OS X machine and do:

iconutil -c Overture.iconset

And that will create Overture.icns.  Make sure the right files are in the Overture.iconset directory (see Apple's docs).
