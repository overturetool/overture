class CPU

types

public Kind = token;

operations

public CPU: nat * Kind ==> CPU
CPU(capacity,kind) ==
  is not yet specified;

public deploy: OBJRef ==> ()
deploy(obj) ==
  is not yet specified;

public setPriority: OBJRef * nat ==> ()
setPriority(obj,prio) ==
  is not yet specified;

end CPU
