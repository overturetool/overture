This directory contains the following support tools:

o a modified version of BYACCJ tool that can create parser table
  initialisation code in Java for extremely large grammars.
	./byaccj

o a special tool is used to create the abstract syntax classes
  for the overture parser from a single descriptive file.
	./astgen

