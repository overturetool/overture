module Eval
exports all
definitions 
operations 

operations

evalExpr : AS`Expression * nat ==> Schd`Val | Schd`BreakInfo
evalExpr(expr, delta)== 
  is not yet specified;

evalSchedule : () ==> ()
evalSchedule() == skip;

end Eval