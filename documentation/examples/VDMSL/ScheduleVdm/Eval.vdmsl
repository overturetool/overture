module Eval
exports all
definitions 
operations 

operations

evalExpr : AS`Expression * nat ==> Schd`Val | Schd`BreakInfo |
                                   Schd`ThreadStatus
evalExpr(expr, delta)== 
  is not yet specified;

evalSchedule : () ==> ()
evalSchedule() == skip;

end Eval